{
  "name": "telegram-text-replacement-bot",
  "private": true,
  "scripts": {
    "start": "python3 -m pip install --user -r requirements.txt && python3 bot.py",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}